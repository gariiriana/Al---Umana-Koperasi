# Generate a long random value and use the exact same value in
# backend/.env.production and wa-gateway/.env.production. Never commit either
# production file.
WA_GATEWAY_API_KEY=replace-with-a-long-random-secret
